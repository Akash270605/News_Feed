<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

<!-- Permission to allow the app to download news from the internet -->
    <uses-permission android:name="android.permission.INTERNET" />

<!-- Permission to show news notifications on newer Android phones -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.News_Feed">

    <!--   The Bookmarks screen     -->
        <activity
            android:name=".SavedArticlesActivity"
            android:label="Saved Articles"
             android:parentActivityName=".MainActivity" />

    <!-- The Main Screen where news is displayed -->
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <!-- This Intent Filter makes MainActivity the "Entry Point" of the app -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>