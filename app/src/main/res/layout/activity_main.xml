<?xml version="1.0" encoding="utf-8"?>

<!--This is the "Home Screen" layout
    A CoordinatorLayout is a special container that helps child views (like a toolbar)
    react to scrolling movements-->
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/backgroundColor"
    tools:context=".MainActivity">

<!--    AppBarLayout is a "Header" container that holds the top bar and tabs-->
    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white"
        app:elevation="0dp">

    <!--  MaterialToolbar is the Top Bar where the app title is show  -->
        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:layout_scrollFlags="scroll|enterAlways"
            app:titleTextColor="@color/colorPrimary"/>

        <!-- TabLayout shows the swipable categroes like "Sports", "Tech", etc. -->
        <com.google.android.material.tabs.TabLayout
            android:id="@+id/tabLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:tabIndicatorColor="@color/colorAccent"
            app:tabIndicatorFullWidth="false"
            app:tabIndicatorHeight="3dp"
            app:tabMode="scrollable"
            app:tabSelectedTextColor="@color/colorAccent"
            app:tabTextColor="@color/textSecondary" />

    </com.google.android.material.appbar.AppBarLayout>

<!--    RecyclerVeiw is a "Scrolling List" that displays many items efficiently-->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvNews"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:paddingBottom="80dp"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

<!--    ExtendedFloatingActionButton is the "Watch Bookmarks" button floating at the bottom -->
    <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
        android:id="@+id/fabBookmarks"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="24dp"
        android:text="Watch Bookmarks"
        android:textColor="@color/white"
        app:icon="@android:drawable/btn_star_big_on"
        app:iconTint="@color/white"
        android:backgroundTint="@color/colorAccent" />

<!--    ProgressBar is the "Loading Spinner" that appears while news is downloading -->
    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>